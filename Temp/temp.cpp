#include <bits/stdc++.h>

using namespace std;

int main() 
{
    if (fopen("loop6.inp", r)) {
        freopen("loop6.inp", "r", stdin);
        freopen("loop6.out", "w", stdout);
    }
    long long n;
    cin >> n;
    long long res = 1;
    for (long long i = 1; i <= n; i++) {
        res *= i;
    }
    co
} 
